image:
  besu:
    tag: 24.3.3-graalvm

quorumFlags:
  privacy: false
  removeKeysOnDelete: false
  isBootnode: false
  usesBootnodes: true

cluster:
  provider: local
  cloudNativeServices: false
  reclaimPolicy: Retain

node:
  besu:
    envBesuOpts: "-XX:MaxRAMPercentage=60.0 -XX:MinRAMPercentage=60.0 -XX:InitialRAMPercentage=60.0 -XX:InitialHeapSize=256M -XX:InitiatingHeapOccupancyPercent=30 -XX:G1HeapRegionSize=16M -XX:NewSize=256M"
    resources:
      cpuLimit: "1"
      cpuRequest: "500m"
      memLimit: "2G"
      memRequest: "2G"
    customLabels:
      nodeType: archive-extra
    metrics:
      serviceMonitorEnabled: false
    sync:
      mode: FULL
    rpc:
      maxActiveConnections: 500
      api: '["DEBUG", "ETH", "ADMIN", "NET", "TRACE", "QBFT", "TXPOOL"]'
    ws:
      api: '["DEBUG", "ETH", "ADMIN", "NET", "TRACE", "QBFT", "TXPOOL"]'

storage:
  pvcSizeLimit: 60Gi

affinity:
  zone:
  - ap-southeast-2b